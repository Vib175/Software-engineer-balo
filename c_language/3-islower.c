#include "main.h"
/**
 * _islower - will print if the character are lower ca
 * @c: The character to be check
 * 	
 * Return: 1 if character is lower case,0 otherwise
*/
int _islowerint(c)
{
	if (c >= 'a' && c <= 'z')                               return (1);
	else                                                    return (0);
}
